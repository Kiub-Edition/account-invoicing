<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account.move.supplier.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <field name="partner_shipping_id" position="before">
                <label for="alternate_payer_id" class="fw-bold" invisible="journal_id.type not in ('sale')" />
                <div class="o_col" invisible="journal_id.type not in ('sale')">
                    <field name="alternate_payer_id" readonly="state != 'draft'" />
                </div>
            </field>
        </field>
    </record>
</odoo>
